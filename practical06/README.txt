This file contains:

main.c:
A program that multiplies two matrices, set in the program program
file, and ouputs the result.

step6:
A directory that contains a program to do the same thing, but
uses a makefile to do keep the multiplication function in a
seperate file, matmul.c
